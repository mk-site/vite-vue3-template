import { defineComponent, reactive } from 'vue';

const About = defineComponent({
    setup() {
        return () => (
            <div>
                home页面
            </div>
        );
    }
});
export default About;